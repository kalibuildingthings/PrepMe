# ElevenLabs API key (server-side only, never exposed to the client)
# Get it from https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# The Agent ID of the "PrepMe Interviewer" Conversational AI agent you create
# in the ElevenLabs dashboard (see README.md for the exact setup: system
# prompt variables, evaluation criteria, and data collection fields).
ELEVENLABS_AGENT_ID=
